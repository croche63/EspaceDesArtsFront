import { CommentaireSalleExposition } from './commentaire-salle-exposition';

describe('CommentaireSalleExposition', () => {
  it('should create an instance', () => {
    expect(new CommentaireSalleExposition()).toBeTruthy();
  });
});
