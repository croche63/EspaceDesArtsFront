import { SignalementSalleVirtuelle } from './signalement-salle-virtuelle';

describe('SignalementSalleVirtuelle', () => {
  it('should create an instance', () => {
    expect(new SignalementSalleVirtuelle()).toBeTruthy();
  });
});
