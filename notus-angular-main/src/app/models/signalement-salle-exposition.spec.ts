import { SignalementSalleExposition } from './signalement-salle-exposition';

describe('SignalementSalleExposition', () => {
  it('should create an instance', () => {
    expect(new SignalementSalleExposition()).toBeTruthy();
  });
});
