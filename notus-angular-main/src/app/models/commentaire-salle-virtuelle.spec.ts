import { CommentaireSalleVirtuelle } from './commentaire-salle-virtuelle';

describe('CommentaireSalleVirtuelle', () => {
  it('should create an instance', () => {
    expect(new CommentaireSalleVirtuelle()).toBeTruthy();
  });
});
